import { it, describe, expect } from 'vitest';
import { i18n } from 'i18next';

import { initI18n } from './i18n.js';

describe('app', () => {
  it.skip('returns i18n instance', () => {
    const result = initI18n();
  });
});
